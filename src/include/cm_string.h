#ifndef CM_STRING_H
#define CM_STRING_H

//concatenates all strings in addArray and returns a pointer if successfull, NULL if else(WIP)
//strings must be null terminated
char* cm_concat(char** addArray, unsigned int addArraySize);

#endif